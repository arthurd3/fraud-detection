### Onda 9 — Rig calibrado LOCAL (CPU-throttle p/ preditor local→Mac Mini).
### Override de docker-compose; NUNCA commitar/NUNCA na submission (poluiria
### o artefato oficial com cpus minúsculos). Apenas dev local.
docker-compose.calib.yml

### Onda 5 PGO — binário instrumentado intermediário (descartável, ~57MB).
### Gerado por Dockerfile.instr (--target export); regerável. Não versionar.
### O profile resultante (api/default.iprof) ESSE sim é versionado.
_pgo/

### Onda 7 v2 — EXACT KD-tree binário (RKD3), baked/offline, regenerável.
### (regra efetiva em api/.gitignore; repetida aqui por completude)
api/src/main/resources/references.kdt
references.kdt

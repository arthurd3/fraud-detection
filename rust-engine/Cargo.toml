[package]
name = "fdsearch"
version = "0.1.0"
edition = "2021"

[lib]
name = "fdsearch"
crate-type = ["staticlib"]   # => libfdsearch.a

[profile.release]
opt-level = 3
lto = true
panic = "abort"              # evita unwinding/symbols extras no link
codegen-units = 1
